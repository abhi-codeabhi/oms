apiVersion: v1
kind: ConfigMap
metadata:
  name: connectorhub-config
data:
  APP_ENV: "production"
  OTEL_EXPORTER_OTLP_ENDPOINT: ""
  ENTITLEMENTS_URL: "http://entitlements:8080"
  # CONNECTOR_KEK is NOT here — it is a secret, supplied via connectorhub-secrets.
